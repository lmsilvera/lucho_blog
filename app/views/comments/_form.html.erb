<%= form_for([@blog, @comment]) do |f| %>
  <%= f.label :comentario %><br>
  <%= f.text_area :comment %><br>

  <%= f.label :autor %><br>
  <%= f.text_field :author %><br>

  <%= f.submit %>
<% end %>